const config = {
    soundUrls: ['./sounds/1.wav', './sounds/2.wav'],
    maxBeat: 16,
    maxTickInTime: 16,
    maxHistoryLength: 4,
    inactivityTime: 3000,
    minBpm: 15,
    maxBpm: 300,
    initialBpm: 60
};

export default config;
